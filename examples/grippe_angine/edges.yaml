# Grippe vs. Angine — shared symptom correlations
#
# Only symptom↔symptom edges go here.
# Disease→symptom edges are declared inline on the disease nodes
# in nodes.yaml.

edges:
  # temperature (d=2) ↔ headache (d=1): 2×1 coupling
  # Fever and headache are correlated regardless of cause.
  # Small coupling since E[T_gauss] values are large.
  - node_a: temperature
    node_b: headache
    coupling:
      - [0.005]
      - [0.0]
